% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_uncertainty}
\alias{plot_uncertainty}
\title{Plot prediction intervals and their coverage}
\usage{
plot_uncertainty(model, level = 0.9, ...)
}
\arguments{
\item{model}{A model fitted by \code{\link[=train_model]{train_model()}}.}

\item{level}{Coverage level.}

\item{...}{Passed to \code{plot}.}
}
\value{
A data frame of predictions, bounds and whether each was covered,
invisibly.
}
\description{
Observations are sorted by prediction and drawn with their conformal
interval, with the ones the interval missed picked out. A calibrated
interval should miss about \code{1 - level} of them, scattered rather than
concentrated at one end.
}
\examples{
p <- agri_project(demo_agri_data(n_units = 20, n_seasons = 3))
p <- build_features(phenology_windows(add_climate(p)), stats = "sum")
m <- train_model(p, "yield", algorithm = "lm", k = 3)
plot_uncertainty(m)
}
\seealso{
\code{\link[=uncertainty]{uncertainty()}}, \code{\link[=predict.agri_model]{predict.agri_model()}}
}
