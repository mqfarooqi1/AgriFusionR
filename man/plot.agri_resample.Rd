% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.agri_resample}
\alias{plot.agri_resample}
\title{Plot a resampling scheme}
\usage{
\method{plot}{agri_resample}(x, ...)
}
\arguments{
\item{x}{An \code{\link[=resample_scheme]{resample_scheme()}} object.}

\item{...}{Passed to \code{plot}.}
}
\value{
\code{x}, invisibly. Called for the plot.
}
\description{
Draws each unit coloured by the fold it is held out in, so that what the
blocking actually did can be seen rather than assumed. Blocks that look
interleaved are not blocking anything.
}
\examples{
p <- agri_project(demo_agri_data(n_units = 24, n_seasons = 2))
p <- build_features(phenology_windows(add_climate(p)), stats = "sum")
plot(resample_scheme(p, method = "spatial_block", k = 4))
}
\seealso{
\code{\link[=resample_scheme]{resample_scheme()}}, \code{\link[=plot.agri_project]{plot.agri_project()}}
}
