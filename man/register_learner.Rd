% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-registry.R
\name{register_learner}
\alias{register_learner}
\title{Register a learning algorithm}
\usage{
register_learner(name, fit, predict, requires = character(), description = "")
}
\arguments{
\item{name}{Name used to select the learner.}

\item{fit}{A function \verb{function(x, y, ...)} returning a fitted model, where
\code{x} is a numeric data frame of predictors and \code{y} a numeric response.}

\item{predict}{A function \verb{function(object, newx, ...)} returning a numeric
vector of predictions.}

\item{requires}{Character vector of packages the learner needs.}

\item{description}{A one-line human description.}
}
\value{
Invisibly, the registered name.
}
\description{
Adds an algorithm to the learner registry so that \code{\link[=train_model]{train_model()}} can select
it by name. Keeping learners behind a registry is what allows the package to
delegate to established implementations rather than reimplement them.
}
\examples{
register_learner("mean_only",
                 fit = function(x, y, ...) mean(y),
                 predict = function(object, newx, ...) {
                     rep(object, nrow(newx))
                 })
"mean_only" \%in\% list_learners()$name
}
\seealso{
\code{\link[=list_learners]{list_learners()}}, \code{\link[=register_source]{register_source()}}
}
