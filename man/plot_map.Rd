% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_map}
\alias{plot_map}
\title{Map predictions, residuals or uncertainty}
\usage{
plot_map(
  model,
  what = c("prediction", "residual", "uncertainty"),
  season = NULL,
  level = 0.9,
  ...
)
}
\arguments{
\item{model}{A model fitted by \code{\link[=train_model]{train_model()}}.}

\item{what}{\code{"prediction"}, \code{"residual"} (observed minus out-of-fold
prediction, on a diverging scale centred at zero), or \code{"uncertainty"}
(the conformal interval half-width, wider where the model is less sure).}

\item{season}{Optional season to show. Defaults to the first, since
overplotting several seasons at one location hides all but the last.}

\item{level}{Coverage level for \code{"uncertainty"}.}

\item{...}{Passed to \code{plot}.}
}
\value{
A data frame of the plotted values, invisibly.
}
\description{
Draws each management unit at its coordinates, coloured by what the model
produced there. The uncertainty map is the one worth reading: a model can
have acceptable average skill and still be useless over part of its area,
and only the map shows where.
}
\examples{
p <- agri_project(demo_agri_data(n_units = 24, n_seasons = 2))
p <- build_features(phenology_windows(add_climate(p)), stats = "sum")
m <- train_model(p, "yield", algorithm = "lm", k = 4)
plot_map(m, "residual")
}
\seealso{
\code{\link[=plot.agri_model]{plot.agri_model()}}, \code{\link[=uncertainty]{uncertainty()}}
}
