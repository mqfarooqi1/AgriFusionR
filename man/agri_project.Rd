% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{agri_project}
\alias{agri_project}
\title{Create an agricultural analysis project}
\usage{
agri_project(
  data,
  unit_id = NULL,
  x = NULL,
  y = NULL,
  season = NULL,
  start = NULL,
  end = NULL,
  crop = NULL,
  crs = 4326,
  cache_dir = NULL
)
}
\arguments{
\item{data}{A data frame with one row per management unit and season.}

\item{unit_id, x, y}{Column names giving the unit identifier and its
coordinates. Detected from names such as \code{unit_id}, \code{field}, \code{lon}, \code{lat}
when \code{NULL}.}

\item{season}{Column naming the season, typically a year. Detected from
\code{season} or \code{year}.}

\item{start, end}{Columns giving the start and end of the growing window as
dates. Detected from names such as \code{planting}/\code{sowing} and \code{harvest}.}

\item{crop}{Column giving the crop, if more than one is present.}

\item{crs}{Coordinate reference system as an EPSG code. Only 4326 is
currently treated as geographic; anything else is taken as projected, which
changes how distances are computed.}

\item{cache_dir}{Directory for cached downloads. Defaults to a session
temporary directory, so nothing is written outside it unless asked.}
}
\value{
An object of class \code{agri_project}.
}
\description{
Builds the object every other function in the package operates on. The unit
of observation is a \strong{management unit within a season}: a field, plot or
administrative area, together with the window over which the crop grew. Each
observation is keyed by \verb{(unit_id, season_id)}, and every covariate layer
added later must reduce to that key.
}
\details{
Column roles are detected from common names when not given explicitly, so
\code{agri_project(data)} usually works unchanged. Detection is reported by the
print method, and can always be overridden.
}
\examples{
d <- demo_agri_data(n_units = 6, n_seasons = 2)
p <- agri_project(d)
p
}
\seealso{
\code{\link[=add_climate]{add_climate()}}, \code{\link[=build_features]{build_features()}}, \code{\link[=check_project]{check_project()}}
}
