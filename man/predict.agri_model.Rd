% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{predict.agri_model}
\alias{predict.agri_model}
\title{Predict from a fitted model}
\usage{
\method{predict}{agri_model}(object, newdata = NULL, interval = FALSE, level = 0.9, ...)
}
\arguments{
\item{object}{A model fitted by \code{\link[=train_model]{train_model()}}.}

\item{newdata}{Optional data frame of features. When omitted, predictions
for the training rows are returned, alongside the out-of-fold predictions,
which are the honest ones.}

\item{interval}{Attach conformal prediction intervals.}

\item{level}{Coverage level for the interval.}

\item{...}{Ignored.}
}
\value{
A data frame with the keys, \code{.pred}, and when \code{newdata} is omitted
\code{.pred_oof}; plus \code{.lower} and \code{.upper} when \code{interval = TRUE}.
}
\description{
Predict from a fitted model
}
\examples{
p <- agri_project(demo_agri_data(n_units = 12, n_seasons = 3))
p <- build_features(phenology_windows(add_climate(p)))
m <- train_model(p, "yield", algorithm = "lm", k = 3)
head(predict(m, interval = TRUE))
}
\seealso{
\code{\link[=uncertainty]{uncertainty()}}
}
