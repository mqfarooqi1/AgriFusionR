% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-registry.R
\name{register_source}
\alias{register_source}
\title{Register a covariate source}
\usage{
register_source(
  name,
  fetch,
  provides,
  kind = c("series", "static"),
  requires_network = TRUE,
  description = ""
)
}
\arguments{
\item{name}{Name used to select the source, for example \code{"power"}.}

\item{fetch}{A function with signature \verb{function(units, seasons, ...)}
returning a data frame. Series sources must return one row per unit and
date, with columns \code{unit_id} and \code{date}; static sources one row per unit,
with column \code{unit_id}.}

\item{provides}{Character vector of the variables the source returns.}

\item{kind}{Either \code{"series"} for time-varying data or \code{"static"} for
values fixed within a unit.}

\item{requires_network}{Whether the source needs internet access. Sources
that do are skipped in tests and examples.}

\item{description}{A one-line human description.}
}
\value{
Invisibly, the registered name.
}
\description{
Adds a data provider to the source registry, making it available to
\code{\link[=add_climate]{add_climate()}}, \code{\link[=add_soil]{add_soil()}} and \code{\link[=add_satellite]{add_satellite()}}. This is the extension
point for new providers: no change to the package is needed.
}
\examples{
register_source("flat",
                fetch = function(units, seasons, ...) {
                    data.frame(unit_id = units$unit_id, elevation = 100)
                },
                provides = "elevation", kind = "static",
                requires_network = FALSE)
"flat" \%in\% list_sources()$name
}
\seealso{
\code{\link[=list_sources]{list_sources()}}, \code{\link[=register_learner]{register_learner()}}
}
