% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.agri_model}
\alias{plot.agri_model}
\title{Plot a fitted model}
\usage{
\method{plot}{agri_model}(
  x,
  type = c("observed", "residuals", "importance"),
  top = 12,
  n_perm = 3,
  ...
)
}
\arguments{
\item{x}{A model fitted by \code{\link[=train_model]{train_model()}}.}

\item{type}{\code{"observed"} plots out-of-fold predictions against the truth with a one to
one line, which is the honest version of the usual fitted-versus-observed
figure;
\code{"residuals"} plots residuals against the prediction, to expose bias that
a single skill number hides;
\code{"importance"} draws out-of-fold permutation importance.}

\item{top}{Number of features for \code{"importance"}.}

\item{n_perm}{Shuffles per feature for \code{"importance"}.}

\item{...}{Passed to \code{plot}.}
}
\value{
\code{x}, invisibly. Called for the plot.
}
\description{
Plot a fitted model
}
\examples{
p <- agri_project(demo_agri_data(n_units = 20, n_seasons = 3))
p <- build_features(phenology_windows(add_climate(p)), stats = "sum")
m <- train_model(p, "yield", algorithm = "lm", k = 3)
plot(m)
plot(m, type = "residuals")
}
\seealso{
\code{\link[=plot_map]{plot_map()}}, \code{\link[=plot_effect]{plot_effect()}}, \code{\link[=plot_uncertainty]{plot_uncertainty()}}
}
