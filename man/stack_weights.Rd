% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learners.R
\name{stack_weights}
\alias{stack_weights}
\title{Stack weights from a fitted ensemble}
\usage{
stack_weights(model)
}
\arguments{
\item{model}{A model fitted by \code{\link[=train_model]{train_model()}} with \code{algorithm = "stack"}.}
}
\value{
A named numeric vector of weights summing to one.
}
\description{
Reports how much each base learner contributed to a \code{"stack"} model, which
is usually more informative than the ensemble's accuracy alone: a stack that
puts all its weight on one learner is telling you the others added nothing.
}
\examples{
p <- agri_project(demo_agri_data(n_units = 14, n_seasons = 3))
p <- build_features(phenology_windows(add_climate(p)), stats = "sum")
set.seed(1)
m <- train_model(p, "yield", algorithm = "stack", k = 3,
                 compare_random = FALSE)
stack_weights(m)
}
\seealso{
\code{\link[=train_model]{train_model()}}, \code{\link[=list_learners]{list_learners()}}
}
