% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasons.R
\name{phenology_windows}
\alias{phenology_windows}
\title{Derive phenological windows from accumulated thermal time}
\usage{
phenology_windows(
  p,
  crop = NULL,
  stages = NULL,
  t_base = NULL,
  t_upper = NULL,
  layer = "climate"
)
}
\arguments{
\item{p}{An \code{\link[=agri_project]{agri_project()}} with a climate layer providing \code{tmin} and \code{tmax}.}

\item{crop}{Crop name used to look up thermal parameters. When \code{NULL}, the
project's crop column is used if present, otherwise \code{"generic"}.}

\item{stages}{Optional data frame of custom thresholds with columns \code{stage}
and \code{gdd_end}, overriding \code{\link[=crop_parameters]{crop_parameters()}}.}

\item{t_base, t_upper}{Optional overrides for the base and upper
temperatures.}

\item{layer}{Name of the climate layer to read.}
}
\value{
The project, with daily stage labels attached to the climate layer
and a window summary available as \code{p$windows}.
}
\description{
Labels every day of every growing season with the phenological stage the
crop had reached by that date, based on accumulated growing degree days, and
summarises the resulting windows.
}
\details{
Aligning covariates to phenology rather than to the calendar is the point of
the exercise. "Rainfall in September" means different things to two crops
sown six weeks apart; "rainfall during grain fill" means the same thing to
both. Aggregation in \code{\link[=build_features]{build_features()}} uses these labels.
}
\examples{
p <- agri_project(demo_agri_data(n_units = 4, n_seasons = 2))
p <- add_climate(p, source = "demo")
p <- phenology_windows(p)
head(p$windows)
}
\seealso{
\code{\link[=growing_degree_days]{growing_degree_days()}}, \code{\link[=crop_parameters]{crop_parameters()}}, \code{\link[=build_features]{build_features()}}
}
