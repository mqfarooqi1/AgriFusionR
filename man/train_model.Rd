% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{train_model}
\alias{train_model}
\title{Train and honestly validate a model}
\usage{
train_model(
  p,
  target,
  algorithm = "auto",
  resampling = NULL,
  method = "spatial_block",
  k = 5,
  buffer = 0,
  compare_random = TRUE,
  ...
)
}
\arguments{
\item{p}{An \code{\link[=agri_project]{agri_project()}} with features built.}

\item{target}{Name of the response column in the project's observations.}

\item{algorithm}{A registered learner, or \code{"auto"} to use \code{"ranger"} when
available and \code{"lm"} otherwise. See \code{\link[=list_learners]{list_learners()}}.}

\item{resampling}{A \code{\link[=resample_scheme]{resample_scheme()}} object. Built automatically when
\code{NULL}.}

\item{method, k, buffer}{Passed to \code{\link[=resample_scheme]{resample_scheme()}} when it builds the
scheme itself.}

\item{compare_random}{Also evaluate with random folds, to quantify the
optimism that random cross-validation would have introduced.}

\item{...}{Passed to the learner's \code{fit} function.}
}
\value{
An object of class \code{agri_model}.
}
\description{
Fits a model to predict \code{target} from the features built for the project,
estimating skill by spatial resampling rather than random folds.
}
\details{
By default the same model is also evaluated with random folds and both
results are reported. The difference between them is the amount by which
random cross-validation would have overstated the model, and it is worth
knowing before any figure is published.

Missing predictor values are filled with the median of the training rows of
each fold, computed inside the fold so that no information crosses the
split.
}
\examples{
p <- agri_project(demo_agri_data(n_units = 12, n_seasons = 3))
p <- add_climate(p, source = "demo")
p <- phenology_windows(p)
p <- build_features(p)
m <- train_model(p, target = "yield", algorithm = "lm", k = 3)
m
}
\seealso{
\code{\link[=resample_scheme]{resample_scheme()}}, \code{\link[=explain]{explain()}}, \code{\link[=uncertainty]{uncertainty()}}, \code{\link[=report]{report()}}
}
